{
  "name": "synchronous-socket",
  "description": "Synchronous socket interface for Unix domain sockets.",
  "version": "0.0.0",
    "main": "index.js",
    "license": "BSD-3-Clause",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build"
  },
  "devDependencies": {
    "node-gyp": "^10.2.0"
  },
  "dependencies": {
    "bindings": "^1.3.0",
    "nan": "^2.11.1"
  }
}
